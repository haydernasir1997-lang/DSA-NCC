class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        s = ""
        for i in digits:
            s += str(i)
        num = int(s)
        add = num +1 
        back = str(add)
        arr = []
        for i in back:
            arr.append(int(i))
        return arr    
